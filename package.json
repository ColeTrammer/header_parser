{
  "name": "header_parser",
  "version": "1.0.0",
  "description": "app that parses headers on http requests",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ColeTrammer",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3"
  }
}
